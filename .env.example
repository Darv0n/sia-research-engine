# ──────────────────────────────────────────────
# Deep Research Swarm — Environment Variables
# ──────────────────────────────────────────────

# --- Required (V1) ---
ANTHROPIC_API_KEY=sk-ant-...
SEARXNG_URL=http://localhost:8080

# --- Search API Keys (V2, optional) ---
EXA_API_KEY=
TAVILY_API_KEY=

# --- Models ---
# OPUS_MODEL=claude-opus-4-6
# SONNET_MODEL=claude-sonnet-4-6

# --- Limits ---
# MAX_ITERATIONS=3
# TOKEN_BUDGET=200000
# MAX_CONCURRENT_REQUESTS=5

# --- Scoring (V2) ---
# AUTHORITY_WEIGHT=0.2
# RRF_K=60

# --- Convergence (V2) ---
# CONVERGENCE_THRESHOLD=0.05

# --- Search Cache (V3) ---
# SEARCH_CACHE_TTL=3600
# SEARCH_CACHE_DIR=.cache/search

# --- Checkpointing (V4) ---
# CHECKPOINT_DB=checkpoints/research.db
# CHECKPOINT_BACKEND=sqlite       # sqlite | postgres | none

# --- Memory (V5) ---
# MEMORY_DIR=memory/

# --- PostgreSQL (V5, required when CHECKPOINT_BACKEND=postgres) ---
# POSTGRES_DSN=postgresql://user:pass@localhost:5432/research

# --- Run Event Log (V6) ---
# RUN_LOG_DIR=runs/

# --- Execution Mode (V6) ---
# MODE=auto                         # auto | hitl
